# Agents

## Capabilities

- **GitHub CLI (`gh`):** This agent can use the GitHub CLI (`gh`) to interact with GitHub repositories, fetch issues, manage pull requests, and perform other GitHub-related tasks.
